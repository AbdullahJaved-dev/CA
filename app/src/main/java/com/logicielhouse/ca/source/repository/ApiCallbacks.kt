package com.logicielhouse.ca.source.repository



class ApiCallbacks {

    /*interface BaseCallback {
        fun onPayLoadError( error : String)
    }
    interface GetPolyLineOptions {
        fun polyLineOptions(polylineOptions: PolylineOptions)
    }

    interface RegisterUserCallback : BaseCallback{
        fun registerUser(registerModel : RegisterModel)
    }

    interface OTPCallback : BaseCallback{
        fun otpStatus(otpMainModel: OtpMainModel)
    }

    interface CarDetailsCallback : BaseCallback{
        fun extractedCarDetails(carDetailsMainModel: CarDetailsMainModel)
    }

    interface CarRegCallback : BaseCallback{
        fun carRegStatus(carRegResponseModel: CarRegResponseModel)
    }

    interface GetProfileCallback : BaseCallback{
        fun profileData(profileMainModel: ProfileMainModel)
    }

    interface SetProfileCallback : BaseCallback{
        fun set_profile(profileMainModel: ProfileMainModel)
    }

    interface SupportCallback : BaseCallback{
        fun getSupportResponse(supportMainModel: SupportMainModel)
    }

    interface NewPasswordCallback : BaseCallback {
        fun newPasswordResponse(newPasswordResponse: NewPasswordResponse)
    }*/
}